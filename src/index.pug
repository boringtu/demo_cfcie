doctype html
html(lang="zh")
	head
		meta(http-equiv="Content-Type" content="text/html; charset=utf-8")
		meta(charset="utf-8")
		meta(http-equiv="X-UA-Compatible", content="IE=edge")
		meta(name="viewport", content="width=device-width,initial-scale=1.0")
		meta(name="renderer", content="webkit|ie-comp|ie-stand")
		link(rel="icon", href="favicon.ico")
		title 客服系统
		link(rel="stylesheet" href="css/index.css")
	body
		<!--[if lte IE 9]>
		link(rel="stylesheet" href="css/ie-blocker.css")
		script(type="text/javascript" src="js/ie-blocker.js")
		<![endif]-->

		noscript.
			<style>
			.noscript {position: fixed; z-index: 99999; top: 0; right: 0; bottom: 0; left: 0; display: flex; justify-content: center; align-items: center;}
			.noscript .mask { position: absolute; z-index: 1; top: 0; right: 0; bottom: 0; left: 0; background-color: rgba(0, 0, 0, 0.5);}
			.noscript .content { position: relative; z-index: 2; border-radius: 5px; background-color: white;}
			.noscript .content p { font-size: 18px; line-height: 80px; padding: 0 20px;}
			</style>
			<div class="noscript"><div class="mask"></div><div class="content"><p>您的浏览器禁用了JS脚本运行，请启用该功能</p></div></div>

		#app
			.tab-box
				button.close
					i.icon.icon-close
				.closing-confirm-box
					span.box-arrow
						i
					p 确定要结束对话吗？
					div
						button(data-type="cancel") 取消
						button(data-type="okey") 确定
			.content-box
				.chat-box
					.queue-msg.
						您好，当前您排在第 #[span.queueNum 0] 位，请耐心等待
					.chat-history
						.unreadCount(data-type="down")
							a(href="javascript:;")
								strong #[span 0] 条新消息
							button.icon.icon-close
						.chat-window
							.loading-history
								i.icon.icon-loading.icon-spin.icon-fast
							.chatWrapper

					.chat-toolbar
						.emoji-picker
							button
								i.icon.icon-face
							div.face-wrapper
								span.box-arrow
									i
								div.face-box
									div
										div.faceItems
											//- span(class="face" v-for="item in twemoji.list" :key="item" @click="insertEmoji(item)" v-html="createEmoji(item)")
						form(enctype="multipart/form-data")
							input(type="file" accept="image/gif, image/jpeg, image/png" name="multipartFile")
							i.icon.icon-picture

					.chat-sendbox
						textarea(
							placeholder="请输入消息..."
							spellcheck="false"
							maxlength="1000"
						)
						button.send(disabled) 发送
				.advertising-box

		script(src="assets/polyfill.min.js")
		script(src="assets/velocity.min.js")
		script(src="assets/twemoji.min.js")
		script(src="assets/jquery.min.js")
		script(src="assets/jquery.form.min.js")
		script(src="assets/sockjs.min.js")
		script(src="assets/stomp.min.js")
		script(src="assets/require.min.js")
		script(src="js/app.js")
